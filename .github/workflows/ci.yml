name: CI

on:
  pull_request:
  push:
    branches:
      - main
    tags:
      - "v*"
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  test:
    name: Test
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v7

      - name: Set up Go
        uses: actions/setup-go@v7
        with:
          go-version-file: go.mod
          cache: false

      - name: Check
        run: make ci

  build:
    name: Build ${{ matrix.goos }}/${{ matrix.goarch }}
    needs: test
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        include:
          - goos: linux
            goarch: amd64
            artifact: palworld-save-reader-linux-amd64
          - goos: linux
            goarch: arm64
            artifact: palworld-save-reader-linux-arm64
          - goos: darwin
            goarch: amd64
            artifact: palworld-save-reader-darwin-amd64
          - goos: darwin
            goarch: arm64
            artifact: palworld-save-reader-darwin-arm64
          - goos: windows
            goarch: amd64
            artifact: palworld-save-reader-windows-amd64.exe

    steps:
      - name: Check out repository
        uses: actions/checkout@v7

      - name: Set up Go
        uses: actions/setup-go@v7
        with:
          go-version-file: go.mod
          cache: false

      - name: Build
        run: make release-build GOOS=${{ matrix.goos }} GOARCH=${{ matrix.goarch }} OUTPUT="dist/${{ matrix.artifact }}"

      - name: Upload executable
        uses: actions/upload-artifact@v7
        with:
          name: ${{ matrix.artifact }}
          path: dist/${{ matrix.artifact }}
          if-no-files-found: error
          retention-days: 7
